# 데스크탑·노트북 연속 작업 정책

## 목적과 적용 범위

이 프로젝트는 데스크탑과 노트북에서 번갈아 작업한다. 대규모·장시간 시뮬레이션은 원칙적으로 데스크탑에서 실행하고, 코드 수정, 문서화, 결과 확인, 소규모 검증은 두 기기 모두에서 수행할 수 있다.

모든 작업의 완료 기준에는 다른 기기에서 바로 이어서 작업할 수 있도록 GitHub와 Obsidian의 상태를 함께 최신화하는 것이 포함된다. 이 정책은 이 디렉터리와 모든 하위 디렉터리에 적용한다.

## 정보의 기준

- 코드, 설정, 자동화 스크립트와 재현 가능한 산출물의 기준은 GitHub 저장소이다.
- 작업 의도, 결정 사항, 실험 요약, 실행 중인 작업과 다음 단계의 기준은 Obsidian 프로젝트 노트이다.
- GitHub 커밋과 Obsidian 작업 기록은 커밋 해시를 통해 서로 연결한다.
- GitHub와 Obsidian의 상태가 다르면 코드는 실제 파일과 Git 이력을 우선하고, 작업 의도와 후속 계획은 Obsidian 기록을 참고한다.

고정 동기화 위치:

- Obsidian Vault: `C:\Users\kevin\Obsidian\LiamObsidian`
- MQSim 노트 폴더: `C:\Users\kevin\Obsidian\LiamObsidian\MQSim SSD 연구`
- MQSim 홈: `C:\Users\kevin\Obsidian\LiamObsidian\MQSim SSD 연구\MQSim Home.md`
- 실험 대시보드: `C:\Users\kevin\Obsidian\LiamObsidian\MQSim SSD 연구\Experiments\Experiment Dashboard.md`
- 다중 기기 정책 노트: `C:\Users\kevin\Obsidian\LiamObsidian\MQSim SSD 연구\Operations\다중 기기 작업 정책.md`
- Obsidian Git 원격: `https://github.com/liamparkzz/LiamObsidian.git`
- MQSim Git 원격: `https://github.com/liamparkzz/MQSim.git`

## 기기별 역할

- 데스크탑: 장시간 시뮬레이션, 대용량 데이터 처리, 성능 측정과 자원 집약적 작업
- 노트북: 코드·설정 수정, 문서화, 작은 테스트, 결과 검토와 후속 작업
- 역할은 기본값일 뿐이며, 실행 기기와 결과 위치를 정확히 기록하면 어느 기기에서도 작업할 수 있다.

## 작업 시작 절차

작업을 시작할 때 반드시 다음 순서로 확인한다.

1. 현재 작업 디렉터리가 올바른 Git 저장소인지 확인한다.
2. `git status`로 현재 브랜치와 미커밋 변경사항을 확인한다.
3. 원격 저장소 상태를 확인하고, 로컬 변경을 보존할 수 있을 때만 최신 변경사항을 가져온다.
4. Obsidian 프로젝트 노트와 `PROJECT_HANDOFF.md`의 최신 기록 및 `다음 작업`을 확인한다.
5. 다른 기기에서 생성된 변경이나 출처를 알 수 없는 로컬 변경은 사용자 소유로 간주하고 보존한다.
6. 충돌이 있으면 양쪽 내용을 보존한다. 임의로 초기화하거나 덮어쓰지 않는다.

자동 pull/push 또는 Obsidian 동기화가 설정되어 있어도 완료되었다고 가정하지 말고 실제 상태를 확인한다.

## 변경 시 필수 동기화

다음 중 하나라도 발생하면 GitHub와 Obsidian 기록을 모두 갱신한다.

- 소스 코드, 스크립트, 설정, 의존성 또는 데이터 처리 방식 변경
- 시뮬레이션 조건, 파라미터, 시드 또는 실행 명령 변경
- 버그 발견이나 수정
- 설계 결정 또는 기존 결정 변경
- 실험 결과나 성능 수치 생성
- 실행 환경 변경
- 미해결 문제 또는 다음 작업 변경

오탈자처럼 의미가 작은 변경은 같은 작업 단위의 기록에 묶을 수 있다.

## GitHub 반영 규칙

1. 작업 전 원격 상태를 확인하고, 작업 후 변경 파일과 diff를 검토한다.
2. 하나의 논리적 작업 단위마다 목적이 드러나는 메시지로 커밋한다.
3. 가능한 테스트 또는 소규모 검증을 수행하고 결과를 기록한다.
4. 테스트하지 못한 항목은 이유와 예상 영향을 명시한다.
5. 작업 종료 전 커밋이 원격 저장소에 반영되었는지 확인한다.
6. 사용자가 명시적으로 요청하지 않은 강제 push, 이력 재작성, 브랜치 삭제, `git reset --hard` 또는 파괴적인 checkout을 수행하지 않는다.
7. API 키, 비밀번호, 토큰, 개인정보와 기타 비밀정보를 커밋하지 않는다.
8. 대용량 원본 데이터와 불필요한 시뮬레이션 산출물은 저장소에 넣지 않는다. 대신 저장 위치, 생성 방법과 요약 결과를 기록한다.
9. 원격 저장소나 인증에 접근할 수 없으면 push를 완료했다고 표현하지 않고, 로컬 커밋 상태와 필요한 후속 조치를 남긴다.

## Obsidian 반영 규칙

Obsidian 프로젝트 노트에는 전체 코드나 장문의 로그를 복사하지 않는다. 핵심 결론, 관련 파일 경로, 결과 위치와 Git 커밋 해시를 기록한다.

작업 종료 시 다음 형식으로 기록을 추가한다.

```text
## YYYY-MM-DD HH:mm 작업 기록

- 작업 기기: 데스크탑 | 노트북 | 확인 불가
- Git 브랜치:
- Git 커밋:
- 작업 목적:
- 완료한 내용:
- 변경한 주요 파일:
- 실행/테스트 결과:
- 시뮬레이션 상태:
- 결과 저장 위치:
- 결정 사항:
- 미해결 문제:
- 다음 작업:
- 재개 명령 또는 참고사항:
```

현재 환경에서 Obsidian Vault에 직접 접근할 수 없으면 다음과 같이 처리한다.

1. 동일한 내용을 저장소의 `PROJECT_HANDOFF.md`에 먼저 기록한다.
2. Obsidian에 반영하지 못한 이유와 복사해야 할 대상을 명시한다.
3. Obsidian 접근이 가능한 다음 작업 환경에서 `PROJECT_HANDOFF.md`의 미반영 기록을 프로젝트 노트로 옮기고 반영 상태를 갱신한다.
4. 실제로 반영하기 전에는 Obsidian 동기화가 완료되었다고 표현하지 않는다.

## 시뮬레이션 실행 규칙

장시간 또는 자원 집약적인 시뮬레이션은 기본적으로 데스크탑에서 실행한다. 실행마다 아래 정보를 기록한다.

- 실행 기기
- 관련 Git 커밋
- 실행 명령
- 설정 파일
- 주요 파라미터와 랜덤 시드
- 시작 시각과 종료 시각
- 결과 저장 경로
- 실행 상태: 예정, 실행 중, 성공, 실패 또는 중단
- 실패 원인과 재개 방법

실행 중인 시뮬레이션은 Obsidian과 `PROJECT_HANDOFF.md`에 `실행 중`으로 표시한다. 다른 기기에서 동일한 출력 경로나 실행 ID를 사용하는 중복 실행을 시작하지 않는다. 완료 또는 중단 시 상태와 핵심 결과를 즉시 갱신한다.

## 충돌 처리

- 동기화 충돌이 발생하면 모든 버전을 먼저 보존한다.
- 출처가 불분명한 변경을 삭제하거나 되돌리지 않는다.
- 자동 병합이 코드 의미, 실험 조건 또는 결과 해석을 바꿀 가능성이 있으면 사용자 확인 없이 병합하지 않는다.
- Obsidian의 동일 노트가 충돌하면 양쪽 기록의 시각, 기기명과 커밋 해시를 비교해 병합하고, 중복 기록만 정리한다.
- 실행 결과 충돌 시 원본을 덮어쓰지 말고 기기명, 시각 또는 실행 ID가 포함된 별도 경로로 보존한다.

## 작업 종료 체크리스트

작업을 종료하기 전에 다음을 모두 확인한다.

- [ ] 변경된 파일과 diff를 검토했다.
- [ ] 필요한 테스트를 실행했거나 미실행 사유를 기록했다.
- [ ] 변경사항을 커밋했다.
- [ ] 커밋의 원격 반영 여부를 확인했다.
- [ ] Obsidian 프로젝트 노트를 갱신했다.
- [ ] `PROJECT_HANDOFF.md`를 갱신했다.
- [ ] 실행 중인 시뮬레이션과 결과 위치를 기록했다.
- [ ] 다른 기기에서 바로 시작할 수 있도록 다음 작업과 재개 명령을 적었다.
- [ ] 커밋되지 않거나 동기화되지 않은 항목을 명시했다.

## 완료 보고 규칙

최종 보고에는 다음을 간단히 포함한다.

- 완료한 변경
- 검증 결과
- Git 브랜치와 커밋/원격 반영 상태
- Obsidian 반영 상태
- 실행 중인 시뮬레이션
- 남은 문제와 다음 작업

GitHub 또는 Obsidian 반영이 불가능하면 작업을 숨기지 말고, 정확히 무엇이 로컬에만 남았는지 명시한다.
