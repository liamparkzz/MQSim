# Laptop Setup

Clone the private repository on the laptop:

```powershell
git clone https://github.com/liamparkzz/MQSim.git
cd MQSim
```

Before starting work:

```powershell
git pull
```

After finishing work:

```powershell
git add .
git commit -m "Describe the change"
git push
```
